apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: particletransformer-pvc
  namespace: cms-ml
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 200Gi