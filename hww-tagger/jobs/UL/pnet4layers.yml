apiVersion: batch/v1
kind: Job
metadata:
  name: weaver-job-rk-ul-pnet-4-layers
  labels:
    app: weavertagger
spec:
  template:
    spec:
      containers:
      - name: container
        image: gitlab-registry.nrp-nautilus.io/raghsthebest/hww-tagger:latest
        command:
        - "/bin/bash"
        - "-c"
        - >
          git clone https://github.com/rkansal47/weaver &&
          cd weaver &&
          mkdir -p /hwwtaggervol/models/rk/02_23_pnet_4_layers &&
          (python -u train.py
          --data-train '/hwwtaggervol/training/ak15_Feb14/train/QCD*/*.root'
          '/hwwtaggervol/training/ak15_Feb14/train/BulkGraviton*/*.root'
          --data-config data/rk/ak15_4q3q_flat_eta_genHm_pt300.yaml
          --network-config networks/particle_net_pf_sv_4_layers.py
          --model-prefix /hwwtaggervol/models/rk/02_23_pnet_4_layers/pnet_4_layers
          --num-workers 12
          --batch-size 256
          --num-epochs 60
          --start-lr 3e-3
          --optimizer ranger
          --gpu "0,1"
          --fetch-step 0.02
          --load-epoch 31
          | tee /hwwtaggervol/logs/rk/02_23_pnet_4_layers_3.txt &
          wait)
        volumeMounts:
        - mountPath: /hwwtaggervol
          name: hwwtaggervol
        resources:
          limits:
            memory: 140Gi
            cpu: "16"
            nvidia.com/gpu: "2"
          requests:
            memory: 128Gi
            cpu: "12"
            nvidia.com/gpu: "2"
      volumes:
      - name: hwwtaggervol
        persistentVolumeClaim:
          claimName: hwwtaggervol
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: gpu-type
                operator: In
                values:
                - "1080"
                - 1080Ti
                - 2080Ti
      restartPolicy: Never
  backoffLimit: 0
